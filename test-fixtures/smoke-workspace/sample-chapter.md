# Chapter One

This is an intro paragraph with $a^2 + b^2 = c^2$ inline math.

:::{note}
An admonition.

With a blank line inside it.
:::

```{code-cell} python
import numpy as np

print(np.pi)
```

A closing paragraph.
