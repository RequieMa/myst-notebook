# Write prose — watch it render

- Type a paragraph → move focus away → it renders in place.
- Math (`$...$`, `$$...$$`), admonitions (`:::{note}`), and figures render inline.
- Press **Enter** at the end of a paragraph to open a new cell below.
- Press **Alt+Enter** to insert a literal newline without splitting.
